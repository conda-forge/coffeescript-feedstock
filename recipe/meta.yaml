{% set name = "coffeescript" %}
{% set version = "2.3.2" %}
{% set sha256 = "fbdb7cb36d368d944ca96ab6b30ede36e6f26c5dc5e1c7d70438270a9ad56dda" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/jashkenas/{{ name }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: generic
  script: |
    tgz=$(npm pack)
    npm install -g $tgz

requirements:
  build:
    - nodejs
  
test:
  commands:
    - npm list -g coffeescript
  requires:
    - nodejs
   
about:
  home: https://github.com/jashkenas/coffeescript
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Unfancy JavaScript'
  description: |
    CoffeeScript is a little language that compiles into JavaScript. 
  doc_url: http://coffeescript.org/
  dev_url: https://github.com/jashkenas/coffeescript
  
extra:
  recipe-maintainers:
    - cshaley
    - sannykr
